

def create_goal(n) ->list:
    """
    Returns as a list the goal to reach for a n-puzzle of size n in a Snale style"
    :param int n: size of puzzle
    :return list: list of size n^2 with the solution
    """


def action(puzzle, i) ->list:
    """
    Returns the state of the puzzle after swapping tile number i. Returns error if tile can not be swapped.
    :param list puzzle:
    :param int i:
    :return list:
    """


def print_puzzle(puzzle) ->None:
    """

    :param list puzzle:
    :return:
    """